# NodeJS-Bookstore-WebApp

## Installation

NodeJS Bokstore WebApp requires [Node.js](https://nodejs.org/) v14+ to run.

Install the dependencies and start the server.

```sh
cd NodeJS_Bookstore_WebApp
npm i
npm start
```
